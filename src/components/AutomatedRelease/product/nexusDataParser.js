/**
 * 解析nexus目录结构数据，用于展示树状结构组件展示类似nexus目录
 * @param {nexus目录结构数据} data
 */
const parser = function (data) {
  let result = []
  if (!data || typeof data !== 'string') {
    return []
  }
  return result
}

export default parser
